<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                        android:orientation="vertical"
                                        android:layout_width="fill_parent"
                                        android:layout_height="fill_parent"
                                        android:id="@+id/navigation_drawer">
    <GridLayout
            android:layout_width="fill_parent"
            android:layout_height="fill_parent" android:columnCount="1"
            android:rowCount="3" android:background="#FFFFFF">
        <Button
                android:layout_width="321dp"
                android:layout_height="180dp"
                android:text="Start Game"
                android:id="@+id/startGameButton" android:layout_row="0" android:layout_column="0"
                android:gravity="center"
                android:layout_gravity="top|center" android:textSize="22pt"
                android:onClick="onStartGameButtonClick"/>
        <Button
                android:layout_width="321dp"
                android:layout_height="180dp"
                android:text="Awards"
                android:gravity="center"
                android:textSize="22pt"
                android:layout_gravity="center"
                android:id="@+id/achievementsButton" android:layout_row="1" android:layout_column="0"
                android:onClick="onAchievementsButtonClick"/>
        <Button
                android:layout_width="321dp"
                android:layout_height="180dp"
                android:text="View Questions"
                android:id="@+id/viewQuestionsButton" android:layout_row="2" android:layout_column="0"
                android:layout_gravity="center_horizontal" android:textSize="22pt"
                android:onClick="onViewQuestionButtonClick"/>
    </GridLayout>
    <!-- The navigation drawer -->
    <ListView android:id="@+id/navigation_drawer_items"
              android:layout_width="240dp"
              android:layout_height="match_parent"
              android:layout_gravity="start"
              android:choiceMode="singleChoice"
              android:divider="@android:color/transparent"
              android:dividerHeight="0dp"
              android:background="#111"/>
</android.support.v4.widget.DrawerLayout>

